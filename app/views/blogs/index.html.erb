<h1> Blogs </h1>
<%= search_form_for @search do |f| %>
  <%= f.label :name_cont %>
  <%= f.text_field :name_cont %>
  <%= f.text_field :tags_name_in %>
  <%= f.hidden_field :status_cont, value: "Verified" %>

  <%= f.submit %>
<% end %>

<table id="blogs">
<thead>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Tag List</th>
  </tr>
</thead>
<tbody>
  <% @blogs.each do |blog| %>
    <tr>
      <td><%= blog.name %></td>
      <td><%= blog.description %></td>
      <td><%= blog.tag_list %></td>
      <%= render :partial => 'blog', :object => blog %>
    </tr>
  <% end %>
</tbody>
</table>

